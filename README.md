# MeshCore MQTT Recorder

**MeshCore MQTT Recorder** is a HACS custom integration for Home Assistant that connects to the Dutch MeshCore LoRa mesh network via MQTT over WebSocket, decodes incoming packets, decrypts hashtag channel messages, and exposes them as sensors and events in Home Assistant. Full installation instructions, configuration reference, and usage documentation will be added here before the first release.
